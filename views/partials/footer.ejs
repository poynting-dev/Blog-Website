</div>

<div class="container border-top footer">
  <footer class="py-5">
    <div class="row">
      

      <div class="col-12">
        <form>
          <h5>Subscribe to our newsletter</h5>
          <p>Monthly digest of whats new and exciting from us.</p>
          <div class="d-flex w-100 gap-2">
            <label for="newsletter1" class="visually-hidden">Email address</label>
            <input id="newsletter1" type="text" class="form-control" placeholder="Email address">
            <button class="btn btn-primary" type="button">Subscribe</button>
          </div>
        </form>
      </div>
    </div>

    <div class="d-flex justify-content-between py-4 my-4 border-top">
      <p>&copy; Priyanshu Dixit. All rights reserved.</p>
      <ul class="list-unstyled d-flex">
        <li class="ms-3"><a class="link-dark" href="https://www.linkedin.com/in/dixitpriyanshu23"><svg class="bi" width="24" height="24"><img src="https://img.icons8.com/color/35/000000/linkedin.png"/></svg></a></li>
        <li class="ms-3"><a class="link-dark" href="https://github.com/poynting-dev/"><svg class="bi" width="24" height="24"><img src="https://img.icons8.com/ios-glyphs/35/000000/github.png"/></svg></a></li>
      </ul>
    </div>
  </footer>
</div>



</body>
<script type="text/javascript">

  var len = 100;
  var p = document.getElementById('truncateMe');
  if (p) {
  
    var trunc = p.innerHTML;
    if (trunc.length > len) {
  
      /* Truncate the content of the P, then go back to the end of the
         previous word to ensure that we don't truncate in the middle of
         a word */
      trunc = trunc.substring(0, len);
      trunc = trunc.replace(/\w+$/, '');
  
      /* Add an ellipses to the end and make it a link that expands
         the paragraph back to its original size */
      trunc += '<a href="#" ' +
        'onclick="this.parentNode.innerHTML=' +
        'unescape(\''+escape(p.innerHTML)+'\');return false;">' +
        '...<\/a>';
      p.innerHTML = trunc;
    }
  }
  
  </script>
  
</html>